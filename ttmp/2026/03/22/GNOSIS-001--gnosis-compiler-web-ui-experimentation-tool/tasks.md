# Tasks

## TODO

- [x] Create Python web server (Flask/FastAPI) with /api/compile endpoint that calls the compiler directly
- [x] Add /api/presets endpoint serving example YAML screens
- [x] Extend compiler to expose intermediate ASTs (per-stage snapshots)
- [ ] Create frontend HTML with YAML editor, canvas, and compile-on-change
- [ ] Implement JS bytecode interpreter (canvas renderer with e-ink palette + bitmap font)
- [ ] Add disassembly view panel
- [ ] Add AST tree viewer with stage switching
- [ ] Add debug overlays (bounds, depth, dirty regions, cross)
- [ ] Add statistics dashboard and manifest viewer
- [ ] Add hex dump view and binary download
- [ ] Add bind simulator panel
- [ ] Style with dark theme matching GNOSIS aesthetic
